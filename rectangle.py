class Rectangle:

    def __init__(self, name, width, height):
    self.name = name
    self.width = width
    self.height = height

    def stretch_horiontal(self):
        self.width *= 2
    def stretch_vertical(self):
        self.height *= 2
    def draw(self):
        rectangle = []
        for index in range(width):
            row = []
            for index in range(height):
                pass
                # * append to row por favor

